{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch CUDA GDB",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/vectorAdd",
            "args": "",
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
        }
    ]
}
